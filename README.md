# My Node App

Simple Express server with validation, tests and Docker setup.

Quick start

Install deps:

```bash
npm install
```

Run server:

```bash
npm start
```

Run tests:

```bash
npm test
```

Docker:

```bash
docker build -t my-node-app .
docker run -p 3000:3000 my-node-app
```
